#pragma once

namespace Config
{
    // Window
    inline constexpr float WIN_WIDTH   = 600.0f;
    inline constexpr float WIN_HEIGHT  = 800.0f;

    // Player
    inline constexpr float USER_SPEED  = 700.0f;

    // Ball
    inline constexpr float BALL_SIZE   = 10.0f;
    inline constexpr float BALL_SPEED  = 200.0f;

    // Fractions
    inline constexpr float QUARTER         = 0.25f;
    inline constexpr float ONE_THIRD       = 0.33f;
    inline constexpr float HALF            = 0.50f;
    inline constexpr float THREE_QUARTERS  = 0.75f;

    // Misc
    inline constexpr int ZERO       = 0;
    inline constexpr int ONE        = 1;
    inline constexpr int FIVE       = 5;
    inline constexpr int MINUS_ONE  = -1;
    inline constexpr int FRAME      = 120;

}
