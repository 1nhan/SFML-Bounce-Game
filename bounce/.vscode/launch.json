{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Bounce (Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Build/Bounce",
            "cwd":"${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}